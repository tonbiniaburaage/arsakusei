# 現在地 → 最終形までの最短実装プラン

## 現在地

以前の版は、1つの `index.html` に次の機能をまとめた2D試作でした。

- 背面カメラの起動
- 低解像度Canvasによる紫色領域の検出
- タップ位置の色を認識色として登録
- Canvasで描いたクラゲ、星、泡、光の重ね表示
- 検出後に約10秒だけ再生する演出

良かった部分は、インストール不要の静的サイト、カメラ許可の流れ、低負荷な検出、作品の雰囲気です。一方、画面座標だけで動く2D表現、10秒で終了する仕様、紫色だけへの依存、単一ファイルへの密結合は最終形に流用できません。また、取得時点のHTMLには日本語の文字化けと壊れた終了タグがありました。

## 残す部分

- GitHub Pagesで配信できる静的Web構成
- `getUserMedia` による背面カメラ取得
- 子供が迷わない「開始」中心のUX
- ドリーミーな紫・ピンク・水色の方向性
- 端末負荷を優先する方針

## 作り直す部分

- Canvas製2Dクラゲ → Three.jsのGLBモデル
- 10秒の単発演出 → AR終了まで継続する更新ループ
- 紫色検出 → 印刷カードまたは台座の画像ターゲット
- 単一ファイル → カメラ、AR、モデル、演出、写真、Configの各責務へ分離
- 1匹専用ロジック → クラゲ、クジラ、ウミガメ共通のConfig方式

## 技術判断

### 無料案（今回の推奨）

Three.js + MindAR Image Tracking + GitHub Pagesを使います。

MindARは静的HTMLから利用でき、Three.jsとの直接統合と画像追跡を提供します。iPhoneとAndroidの通常ブラウザで同じ構成を使いやすく、1週間未満の展示制作では最も事故が少ない選択です。作品カードまたは台座の高コントラスト画像をターゲットにし、そこを3D空間の原点として生物を配置します。

制約は、端末のSLAMで部屋全体を記憶する方式ではなく、基本的に画像ターゲット基準の追跡になることです。ターゲットが画面内か近くに見える撮影構図を展示側で作ります。短時間ターゲットを見失った場合は、最後の姿勢を少し保持して急に消えない演出にします。

### 有料案（真のWorld Trackingが必須の場合）

商用WebAR SDKの利用を検討します。iOS Safariを含め、画像認識後にSLAMへ移行してターゲットを画面外にしても空間固定する体験は、こちらの方が確実です。契約、料金、透かし、公開ドメイン、同時アクセス条件を確認してから採用します。ユーザー承認なしには導入しません。

### 通常のWebXRだけを採用しない理由

Three.jsはWebXR AR用の機能を持ちますが、Appleの案内ではiOSの `immersive-ar` は公式サポート外です。Android Chromeだけを対象にするなら有効ですが、今回の必須条件であるiPhoneとの同一体験を満たせないため、共通の本番経路にはしません。

## Phaseと実機確認条件

### Phase 1 — カメラ + 3D 1匹

状態: 実装済み。

- 背面カメラ上にThree.jsの3Dモデルを1匹表示
- GLBがなければ仮の立体クラゲを表示
- GLB内のアニメーションを自動再生
- PixelRatioを最大1.6に制限

実機確認: GitHub PagesのHTTPS URLを開き、カメラ許可後に仮クラゲが表示され続けること。

### Phase 2 — 画像ターゲットへの固定

状態: 仮ターゲットで実装済み。

- MindAR Image Trackingを導入済み
- 公式デモカードの `.mind` ターゲットを仮配置
- ターゲット位置を原点に設定
- 追跡ロスト時は15フレーム猶予を持たせて再取得

実機確認: スマホを左右・上下に動かしたときに見える角度が変わり、カード上の同じ位置に留まること。

### Phase 3 — 4〜5匹の自然な遊泳

状態: クラゲで実装済み。

- 大1、中2、小2
- 位相、速度、旋回、奥行き、開始時刻を個別化
- 中央30〜40%を空ける遊泳範囲
- 低ポリゴンの仮モデルとPixelRatio制限を使用

実機確認: 同じ軌道のコピーに見えず、中央に人物が入れること。

### Phase 4 — 写真撮影

- 左下に丸い撮影ボタン
- カメラ映像 + WebGL + 2D演出を1枚へ合成
- iOSでは共有シートを優先し、非対応時は画像表示・保存へフォールバック
- 負荷が許せば1〜2秒の集合演出

実機確認: iPhone/Androidの両方で、人物とARが同じ画像に入ること。

### Phase 5 — 星・泡・光

- 各生物の近くに限定した軽量パーティクル
- 上限数と寿命を設定
- 3Dモデル70%、演出30%の見た目を維持

実機確認: 連続使用で重くならず、モデルが主役に見えること。

### Phase 6 — 3種切替

- Configでモデル、数、サイズ、速度、範囲、演出、アニメーションを切替
- URLまたは画像ターゲットごとに生物を選択
- クジラとカメに専用の軌道プリセットを追加

実機確認: コード変更なしで3種類を切り替えられること。

### Phase 7 — 展示安定化

- 実物の照明・距離でターゲットを再撮影
- 追跡の閾値と案内表示を調整
- 低性能端末向け品質設定
- オフラインに近い回線状況を想定して依存ファイルを固定

実機確認: 展示予定の複数端末で、初回起動から撮影までを繰り返すこと。

## 参考

- Apple Developer Forums: https://developer.apple.com/forums/thread/756850
- Three.js ARButton: https://threejs.org/docs/pages/ARButton.html
- MindAR: https://hiukim.github.io/mind-ar-js-doc/
- MindAR installation: https://hiukim.github.io/mind-ar-js-doc/installation/
